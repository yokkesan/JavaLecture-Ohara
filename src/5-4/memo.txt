	•	メソッド名は全部 add
	•	引数の型が違うので、Javaが自動で使うメソッドを選ぶ
→ これが オーバーロード

1. メソッドのシグネチャとは

**メソッドのシグネチャ（signature）**とは、
Java が どのメソッドを呼ぶか判断するための情報 です。

シグネチャに含まれるもの

メソッド名 + 引数の個数 + 引数の型

オーバーロードとは、

同じメソッド名で、シグネチャが異なるメソッドを複数定義すること
