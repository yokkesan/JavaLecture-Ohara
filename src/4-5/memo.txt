① メモリと変数

メモリ
	•	プログラム実行中に 値を置く場所

変数
	•	メモリ上の値に付けた名前

int a = 10;

メモリ上に「10」を置く
↓
その場所に a という名前を付ける

a ──▶ 10

② メモリと配列

配列の正体
	•	配列の中身（要素）は メモリ上の別の場所に並んでいる
	•	配列変数は その場所の番号（参照）を持つ

int[] arrayA = {1, 2, 3};