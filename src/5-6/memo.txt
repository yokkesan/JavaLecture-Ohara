Javaのコマンドライン引数とは、

👉 プログラム起動時に、外から渡す値
です。

⸻

ひとことで

「main に実行時に渡される文字列の配列」


public static void main(String[] args)

	•	args ← コマンドライン引数
	•	型は 必ず String[]
	•	実行時に渡した値が 順番通りに入る


 OSからコマンドで渡せるのは文字列だけ
👉 だから Java の入口は main(String[] args) に固定された

